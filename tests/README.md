# Tests

Test suites and fixtures.

## Harness protocole (PoC)

```bash
python3 -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
python protocol_harness.py --ws ws://localhost:8999/ws
```
